Flask>=3.0
anthropic>=0.40  # only needed for the optional chatbot page
scikit-learn>=1.4  # Predictive Asset Health Score (health_score_service.py)
numpy>=1.26        # feature vectors for the health-score model
